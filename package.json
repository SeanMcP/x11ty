{
  "name": "x11ty",
  "version": "1.0.0",
  "description": "Experiments in 11ty",
  "scripts": {
    "start": "eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.1"
  }
}
